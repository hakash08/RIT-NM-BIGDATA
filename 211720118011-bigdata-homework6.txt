 										            211720118011    Homework 6

Q1. What is Spark SQL?
	Spark SQL is a Spark module for structured data processing. It provides a programming abstraction called dataframes and also act as distributed sql query machine. it is run upto 100x faster on existing deployment and data.It provides support for various data sources and helps to construct SQL queries with code transformations.

Q2. Is there a module to implement SQL in Spark? How it works?

	> Using SparkSession it can be access Spark functionality by importing the class and create an instance in the code. To issue any SQL query, use the sql() method on the SparkSession instance, spark , such as spark. sql("SELECT * FROM myTableName") . 
	> Spark SQL provides DataFrame APIs which perform relational operations on both external data sources and Spark’s built-in distributed collections. Spark runs on both Windows and UNIX-like systems. this can run locally by an installation in java on our system path.


Q3. What is a Parquet file?

	Parquet is an open source file format file built to handle flat columnar storage formats. Parquet operates well with complex data in large volumes.
                  It is known for its both performable data compression and its ability to handle a wide variety of encoding types.

Q4. List the functions of Spark SQL.

	>String Functions
	>Math Functions
	>Aggregate Functions
	>Collection Functions
	>Date & Time Functions
	>Window Functions.

Q5. How is Spark SQL different from HQL and SQL?

	Hive Query Language (HQL) it is used for data stored in Hadoop Distributed File System (HDFS) whereas Spark SQL makes use of structured query                   language. 
                 And makes sure all the read and write online operations.


Q6. Why is Spark SQL used?

	Spark SQL it is used because of memory computing which results better performance. It includes the cost based optimizer, columnar storage and code generation for faster execution of queries. Spark SQL has various performance tuning options like memory settings, codegen, batch sizes and compression codes.

Q7. Is Spark SQL faster than Hive?

	Spark SQL is faster than Hive when it comes to processing speed because the operations in Hive are slower than  Spark SQL in terms of memory and disk processing as Hive runs on top of Hadoop.it is 10x faster on disk than hadoop

